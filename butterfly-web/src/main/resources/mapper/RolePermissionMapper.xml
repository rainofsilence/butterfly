<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silence.butterfly.web.module.sys.mapper.RolePermissionMapper">

    <resultMap id="BaseResultMap" type="cn.silence.butterfly.web.module.sys.model.entity.RolePermission">
            <id property="roleId" column="role_id" jdbcType="VARCHAR"/>
            <id property="mpId" column="mp_id" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        role_id,mp_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from role_permission
        where  role_id = #{roleId,jdbcType=VARCHAR} AND mp_id = #{mpId,jdbcType=VARCHAR} 
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from role_permission
        where  role_id = #{roleId,jdbcType=VARCHAR} AND mp_id = #{mpId,jdbcType=VARCHAR} 
    </delete>
    <insert id="insert">
        insert into role_permission
        ( role_id,mp_id)
        values (#{roleId,jdbcType=VARCHAR},#{mpId,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective">
        insert into role_permission
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="roleId != null">role_id,</if>
                <if test="mpId != null">mp_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="roleId != null">#{roleId,jdbcType=VARCHAR},</if>
                <if test="mpId != null">#{mpId,jdbcType=VARCHAR},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="cn.silence.butterfly.web.module.sys.model.entity.RolePermission">
        update role_permission
        <set>
        </set>
        where   role_id = #{roleId,jdbcType=VARCHAR} AND mp_id = #{mpId,jdbcType=VARCHAR} 
    </update>
    <update id="updateByPrimaryKey" parameterType="cn.silence.butterfly.web.module.sys.model.entity.RolePermission">
        update role_permission
        set 
        where   role_id = #{roleId,jdbcType=VARCHAR} AND mp_id = #{mpId,jdbcType=VARCHAR} 
    </update>
</mapper>
