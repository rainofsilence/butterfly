#日志
logging:
  config: classpath:logback-spring.xml
spring:
  redis:
    host: localhost
    port: 6379
    database: 0
    password: butterfly@2023
    lettuce:
      pool:
        max-idle: 16
        max-active: 32
        min-idle: 8
  session:
    store-type: redis
    timeout: 600 # 600秒
    redis:
      namespace: butterfly:session
mybatis:
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl